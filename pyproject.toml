[project]
name = "webpulse"
version = "0.1.0"
description = "High-performance async web port scanner — find live HTTP/HTTPS services fast"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.0",
    "rich>=13.0.0",
]
